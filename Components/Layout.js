import PokemonCard from "@/Components/pokemonCard";
const Layout = ({children}) => {
    return ( 
        <>
            
            <header/>
            <main>{children}</main>
            
            
        </>
     );
}
 
export default Layout;